{
  "routes": [
    {
      "method": "GET",
      "path": "/defaults",
      "handler": "defaults.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/defaults/count",
      "handler": "defaults.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/defaults/:id",
      "handler": "defaults.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/defaults",
      "handler": "defaults.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/defaults/:id",
      "handler": "defaults.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/defaults/:id",
      "handler": "defaults.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
