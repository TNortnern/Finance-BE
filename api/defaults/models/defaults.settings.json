{
  "kind": "collectionType",
  "collectionName": "defaults",
  "info": {
    "name": "defaults"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "view_count": {
      "type": "integer"
    }
  }
}
